#stable diffusion

ML model used to generate image from text prompt.

LDM (latent diffusion model)

This happens in latent space- where calculations are more faster & easier

CLIP- contrastive language image pre-training dev by openAI

The prompts guides the nisy image, and denoises them
